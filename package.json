{
  "name": "devforces",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test":"echo yet to write the tests",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "start:web": "cd apps/web && pnpm run start",
    "start:backend": "cd apps/backend && pnpm run start",
    "db:migrate": "cd packages/db && pnpm dlx prisma@6.3.0 generate && cd ../..",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.7.4",
    "turbo": "^2.6.3",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
